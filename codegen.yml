overwrite: true
schema: '${NEXT_PUBLIC_STRAPI_API_URL:http://localhost:1337}/graphql'
documents: 'graphql/documents/**/*.{graphql,js,ts,jsx,tsx}'
generates:
  models/generated.ts:
    plugins:
      - 'typescript'
      - 'typescript-operations'
hooks:
  afterAllFileWrite:
    - prettier --write
